[build]
  command = "npm ci && npm run build"
  publish = "dist"
environment = { VITE_API_URL = "https://cloudnova.onrender.com/api/v1" }
# Single-page app routing fallback
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
